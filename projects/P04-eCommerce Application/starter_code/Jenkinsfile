pipeline {
    agent any
    tools {
        maven 'Maven 3.1.1'
        jdk 'jdk8'
    }
    stages {

        stage ('Build') {
            steps {
                sh 'mvn -f "projects/P04-eCommerce Application/starter_code/pom.xml" install'
            }
            post {
                success {
                    junit 'target/surefire-reports/**/*.xml'
                }
            }
        }
    }
}